node_modules
# Keep environment variables out of version control
.env
.env.development
.env.production
.env.test
.env.example

# Frontend development
frontend/node_modules
frontend/.vite

# Frontend production build (deployed to Azure)
# Uncomment if you want to commit the build, otherwise build on Azure
# frontend/dist

/src/generated/prisma
